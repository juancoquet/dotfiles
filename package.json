{
  "name": "dotfiles",
  "private": true,
  "workspaces": ["packages/*"],
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "test": "npm run test --workspace=@dotfiles/pi-workspace-manager",
    "typecheck": "npm run typecheck --workspace=@dotfiles/pi-workspace-manager"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
