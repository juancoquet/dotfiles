.DS_Store
node_modules/
.pi-subagents/

# tmux plugins are installed locally via tpm, not vendored
configs/tmux/plugins/

# runtime state written by yabai scripts, not config
configs/yabai/.ghostty-width-mode
